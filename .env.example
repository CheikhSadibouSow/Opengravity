# Telegram Configuration
TELEGRAM_BOT_TOKEN="SUBSTITUER PAR LE VÔTRE"
# Format CSV pour l'array d'IDs autorisés
TELEGRAM_ALLOWED_USER_IDS="SUBSTITUTE PAR LE VÔTRE"

# LLM Configuration
GROQ_API_KEY="SUBSTITUER PAR LE VÔTRE"
OPENROUTER_API_KEY="SUBSTITUER PAR LE VÔTRE"
OPENROUTER_MODEL="openrouter/free"

# Database Configuration
DB_PATH="./memory.db"

# Optional Google Configuration
# LOCAL DEV: Laissez vide si vous utilisez un fichier `service-account.json` physique à la racine.
# PRODUCTION CLOUD: Remplissez ici avec le contenu JSON aplati de votre clé (stringifiée).
FIREBASE_SERVICE_ACCOUNT_JSON=""
